- name: A playbook that creates service
  hosts: localhost
  user: ubuntu
  become: yes

  tasks:
  - name: 1. Create a service
    shell:
      cmd: kubectl expose deployment/coursework-deployment --type="NodePort" --port 3000 --name node-port-service

  - name: 2.Get services
    shell:
      cmd: kubectl get services
